# 📋 لیست فایل‌هایی که باید به GitHub اضافه کنید

## ✅ فایل‌های ضروری (باید آپلود شوند):

### فایل‌های اصلی پروژه:
- ✅ `package.json` - وابستگی‌های پروژه
- ✅ `package-lock.json` - قفل نسخه‌های وابستگی
- ✅ `tsconfig.json` - تنظیمات TypeScript
- ✅ `tsconfig.node.json` - تنظیمات TypeScript برای Node
- ✅ `vite.config.ts` - تنظیمات Vite
- ✅ `index.html` - فایل HTML اصلی

### پوشه src (تمام محتویات):
- ✅ `src/` - تمام فایل‌های منبع
  - ✅ `src/App.tsx` و `src/App.css`
  - ✅ `src/main.tsx`
  - ✅ `src/index.css`
  - ✅ `src/components/` - تمام کامپوننت‌ها
  - ✅ `src/contexts/` - تمام Context‌ها
  - ✅ `src/utils/` - تمام utility ها

### فایل‌های مستندات:
- ✅ `README.md` - راهنمای پروژه
- ✅ `.gitignore` - فایل ignore

---

## ❌ فایل‌هایی که نباید آپلود شوند:

### پوشه‌های بزرگ (خودکار ignore می‌شوند):
- ❌ `node_modules/` - وابستگی‌های نصب شده (خیلی بزرگ است!)
- ❌ `dist/` - فایل‌های build شده

### فایل‌های سیستم:
- ❌ `.DS_Store` - فایل سیستم Mac
- ❌ `Thumbs.db` - فایل سیستم Windows
- ❌ `.vscode/` - تنظیمات VS Code (به جز extensions.json)
- ❌ `.idea/` - تنظیمات IntelliJ/WebStorm

### فایل‌های لاگ و موقت:
- ❌ `*.log` - فایل‌های لاگ
- ❌ `npm-debug.log*`
- ❌ `yarn-debug.log*`

### فایل‌های محیطی:
- ❌ `.env` - متغیرهای محیطی (اگر دارید)
- ❌ `.env.local`
- ❌ `.env.production`

---

## 📝 راهنمای آپلود دستی:

### روش 1: استفاده از GitHub Web Interface

1. به repository خود در GitHub بروید
2. روی "uploading an existing file" کلیک کنید
3. فقط فایل‌های ✅ را drag & drop کنید
4. از آپلود `node_modules` و `dist` خودداری کنید

### روش 2: استفاده از Git (توصیه می‌شود)

```bash
# بررسی وضعیت
git status

# اضافه کردن فقط فایل‌های لازم (gitignore خودکار فیلتر می‌کند)
git add .

# Commit
git commit -m "Add Habit Tracker app"

# Push
git push origin main
```

---

## ⚠️ نکات مهم:

1. **هرگز `node_modules` را آپلود نکنید** - این پوشه خیلی بزرگ است (بیش از 100MB)
2. **`dist` را آپلود نکنید** - این فایل‌های build شده هستند
3. **فقط فایل‌های منبع (source code) را آپلود کنید**
4. **`.gitignore` را حتماً اضافه کنید** - این فایل از آپلود فایل‌های غیرضروری جلوگیری می‌کند

---

## 📦 بعد از آپلود:

بعد از آپلود، هر کسی که پروژه را clone کند باید:

```bash
git clone https://github.com/YOUR_USERNAME/habit-tracker.git
cd habit-tracker
npm install  # این دستور node_modules را می‌سازد
npm run dev  # اجرای برنامه
```

